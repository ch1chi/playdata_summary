void는 '빈 공간' , 아무것도 없다 라는 의미를 가진 단어 ==> 함수가 아무것도 반환하지 않을 때 사용
즉, 리턴 값이 없을 때 사용한다.  아무것도 반환하지 않는 함수는 void값을 가지고, void 타입인 함수는 undefined를 리턴한다.

** undefined 는 리턴 문이 있어야 하고, void는 리턴 문이 필요없다 ( 왜와? 아무것도 반환하지 않아서 )

API란???



v-if는 조건이 변경되면 조건 안에서 DOM 엘리먼트를 처음부터 다시 그린다. 
// 반면, v-show는 모든 조건의 DOM 엘리먼트를 그린 후에 조건에 맞지 않는 엘리먼트는 다시 hide 처리한다 즉 안보이게 만들어 둔다

v-if는 빠르게 애플리케이션의 그림을 그려주지만, 조건이 변경될 때마다 다시 해당 엘리먼트를 다시 그려야한다  
그러나 v-show는  조금 늦게 그릴지 몰라도 조건이 자주 변경될 때에는 매우 빠른 전환이 이루어진다
따라서 조건이 자주 안바뀔 경우에는 v-if가 유리하고, 조건이 자주 바뀌면 v-show가 더 유리하다


watch는 개발자가 코드로 지정한 변수값의 변화를 감시해 콜백함수로 하여금 부가적인 작업을 할 수 있도록 해준다. 
watch는 지정된 특정한 변수의 감시와 더불어 값이 변경되기 이전 값을 참조(비교하여 대조해본다)할 수 있다는 강력한 장점
컴포넌트가 생성되어 변수가 할당(배분되어 나누기)되기 전에 모든 변수는 null값을 가지고 있다.
watch는 기본적으로 이러한 null에서 초기값으로 넘어가는 과정을 데이터 변경으로 판단하지 않는다.


computed = 원하는 대로 데이터를 변경해주는 강력한 기능
캐쉬(cache)는 잠시 저장해 둔다는 의미를 가진 기능
즉, 로컬 장소에 파일을 미리 받아놓는 것. 
computed 프로퍼티는 vue 인스턴스 내에서 this 키워드와 연결됩니다.  computed 프로퍼티는 data 프로퍼티 / methods와 같은 scope에 존재함
computed 프로퍼티는 기본적으로 getter만 있지만 , 필요하다면 setter도 가능
그러긴 위해선 object으로 변경해줘야함
get ==> 값을 확인하거나 가져올 때 사용 /// set은 값을 설정하거나 변경할 때 사용한다.

------------- 교육( 14시 30분 ) ------------------
인텔리제이 설치 , 마리아DB 설치 , DBeaver 설치 , JDK11 버젼 설치

