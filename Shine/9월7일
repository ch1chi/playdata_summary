

for..in은 모든 열거 가능한 객체 (객체 반복에서 사용)

+상속된 열거 가능한 속성들을 포함하여 객체에서 문자열로 지정된 모든 열거 가능한 속성에 대해 반복한다. 

+배열에서는 사용하지 않는 게 권장 왜냐하면 배열은 인덱스의 순서가 중요한 객체인데 반복문은 순서 없이 반복되는 특징을 가지기 때문에 배열의 인덱스 순서대로 반복한다는 결과를 보장할수없음

배열 말고 순서없는 object자료형에서 사용하는 것이 좋음

눈에 보이지 않는 배열 원소 뿐 아니라 프로토 타입의 속성의 인덱스까지 모두 반복해주기 때문에 디버깅 할 때 유용하다.

for..of는 컬렉션 전용이다.  (배열의 반복에서 사용)

+반복 가능한 객체 (array,map,set,string,typearray,arguments객체 등을 포함)에 대해서 반복하고, 각 개별 속성  값에 대해 실행되는 문이 있는 사용자 정의 반복 후크를 호출하는 루프를 생성함…

배열이 아닌 객체를 사용하면 typeerror을 반환
